local Workspace = game:GetService("Workspace")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local localPlayer = Players.LocalPlayer
local character = localPlayer.Character or localPlayer.CharacterAdded:Wait()
local humanoidRootPart = character:WaitForChild("HumanoidRootPart")

local autoDungeonActive = false
local currentTarget = nil
local lastTarget = nil 
local justDied = false 
local characterLoaded = true -- Control maestro de carga física
local attackHeight = 30      -- Altura inicial de ataque

-- Acceso directo a los eventos de red del juego (Método Pro)
local commF = ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("CommF_")

-- DETECCIÓN PERFECTA DE HAKI (Busca el objeto oficial de Blox Fruits)
local function IsHakiActive()
    if not character then return false end
    -- "HasBuso" es la marca oficial que pone el juego en tu personaje al activar Haki
    if character:FindFirstChild("HasBuso") then
        return true
    end
    return false
end

-- ACTIVACIÓN INSTANTÁNEA POR SERVIDOR (Sin simular teclas)
local function ForceActivateHaki()
    if not character or not autoDungeonActive or not characterLoaded then return end
    
    -- Si no está activo, le ordenamos directamente al servidor que lo encienda
    if not IsHakiActive() then
        pcall(function()
            commF:InvokeServer("Buso")
        end)
    end
end

-- DETECCIÓN DE REAPARICIÓN ESTRICTA
localPlayer.CharacterAdded:Connect(function(newCharacter)
    characterLoaded = false -- Pausa temporal de seguridad
    justDied = true         -- Prioriza el portal de la sala
    
    local humanoid = newCharacter:WaitForChild("Humanoid")
    local rootPart = newCharacter:WaitForChild("HumanoidRootPart")
    
    -- Espera a que el avatar esté completamente dentro del mapa
    while not (newCharacter.Parent == Workspace and humanoid and rootPart and humanoid.Health > 0 and rootPart.Position.Magnitude > 10) do
        task.wait(0.1)
    end
    
    -- Un microsegundo para estabilizar las físicas
    task.wait(0.5) 
    
    character = newCharacter
    humanoidRootPart = rootPart
    characterLoaded = true 
    
    -- Activa el Haki al instante de reaparecer
    if autoDungeonActive then
        ForceActivateHaki()
    end
end)

local netFolder = ReplicatedStorage:WaitForChild("Modules"):WaitForChild("Net")
local registerAttack = netFolder:WaitForChild("RE/RegisterAttack")
local registerHit = netFolder:WaitForChild("RE/RegisterHit")
local enemiesFolder = Workspace:FindFirstChild("Enemies") or Workspace

-- Equipar Espada
local function ForceEquipSword()
    if not character or not autoDungeonActive or not characterLoaded then return end
    
    local currentTool = character:FindFirstChildOfClass("Tool")
    if currentTool and (currentTool.ToolTip == "Sword" or string.find(string.lower(currentTool.Name), "sword") or string.find(string.lower(currentTool.Name), "katana")) then
        return
    end
    
    local backpack = localPlayer:FindFirstChildOfClass("Backpack")
    if backpack then
        for _, tool in ipairs(backpack:GetChildren()) do
            if tool:IsA("Tool") and (tool.ToolTip == "Sword" or string.find(string.lower(tool.Name), "sword") or string.find(string.lower(tool.Name), "katana")) then
                local humanoid = character:FindFirstChildOfClass("Humanoid")
                if humanoid then
                    humanoid:EquipTool(tool)
                    break
                end
            end
        end
    end
end

-- Hilo único de buffs (Espada y Haki)
local function StartAutoBuffs()
    task.spawn(function()
        while autoDungeonActive do
            ForceEquipSword()
            ForceActivateHaki()
            task.wait(0.3) -- Comprobación constante y limpia cada 0.3 segundos
        end
    end)
end

-- Obtener enemigo más cercano (Ignorando la Sombra y Blank Buddy)
local function GetClosestEnemy()
    if justDied or not characterLoaded then return nil end 

    local closestEnemy = nil
    local shortestDistance = math.huge
    local currentCharacter = localPlayer.Character
    
    if not currentCharacter or not currentCharacter:FindFirstChild("HumanoidRootPart") then 
        return nil 
    end
    
    local myPos = currentCharacter.HumanoidRootPart.Position
    local sourceFolder = Workspace:FindFirstChild("Enemies") or enemiesFolder
    
    if sourceFolder then
        local children = sourceFolder:GetChildren()
        local primaryCandidates = {}
        local backupCandidates = {}
        
        for i = 1, #children do
            local object = children[i]
            
            -- CONDICIÓN MANTENIDA: Ignora Blank Buddy, Sombra y Shadow
            if object:IsA("Model") and object ~= currentCharacter and object.Name ~= "Blank Buddy" and object.Name ~= "Sombra" and object.Name ~= "Shadow" then
                local humanoid = object:FindFirstChildOfClass("Humanoid")
                local root = object:FindFirstChild("HumanoidRootPart") or object:FindFirstChild("Head")
                
                if humanoid and humanoid.Health > 0 and root and object.Parent then
                    local distance = (root.Position - myPos).Magnitude
                    if object ~= lastTarget then
                        table.insert(primaryCandidates, {model = object, dist = distance})
                    else
                        table.insert(backupCandidates, {model = object, dist = distance})
                    end
                end
            end
        end
        
        local targetList = #primaryCandidates > 0 and primaryCandidates or backupCandidates
        
        for i = 1, #targetList do
            local candidate = targetList[i]
            if candidate.dist < shortestDistance then
                shortestDistance = candidate.dist
                closestEnemy = candidate.model
            end
        end
    end
    return closestEnemy
end

local function TeleportToEnemy(enemy)
    if not enemy or not enemy.Parent or not humanoidRootPart or not characterLoaded then return false end
    
    local enemyRoot = enemy:FindFirstChild("HumanoidRootPart") or enemy:FindFirstChild("Head")
    if not enemyRoot then return false end
    
    local success, err = pcall(function()
        humanoidRootPart.CFrame = enemyRoot.CFrame * CFrame.new(0, attackHeight, 0)
    end)
    
    if not success then
        local raycastParams = RaycastParams.new()
        raycastParams.FilterDescendantsInstances = {character, enemy}
        raycastParams.FilterType = Enum.RaycastFilterType.Exclude
        
        local raycastResult = Workspace:Raycast(enemyRoot.Position, Vector3.new(0, -100, 0), raycastParams)
        if raycastResult then
            humanoidRootPart.CFrame = CFrame.new(raycastResult.Position + Vector3.new(0, 3, 0))
        end
        return false
    end
    return true
end

local function AttackEnemy(enemy)
    if not enemy or not enemy.Parent or not characterLoaded then return end
    
    local enemyHead = enemy:FindFirstChild("Head")
    local enemyHumanoid = enemy:FindFirstChildOfClass("Humanoid")
    if not enemyHead or not enemyHumanoid then return end
    
    local tool = character:FindFirstChildOfClass("Tool")
    if tool then tool:Activate() end
    
    pcall(function()
        local targetsTable = {{enemy, enemyHead}}
        registerAttack:FireServer(0)
        registerHit:FireServer(enemyHead, targetsTable)
    end)
end

-- Identifica en qué sala física se encuentra parado el jugador actualmente
local function GetCurrentRoomIndex(rooms)
    if not humanoidRootPart then return 1 end
    local myPos = humanoidRootPart.Position
    local currentRoomIdx = 1
    local minDistanceToRoomCenter = math.huge
    
    for i, room in ipairs(rooms) do
        local roomPart = room:FindFirstChildOfClass("Part") or room:FindFirstChildOfClass("MeshPart") or room:FindFirstChild("Floor")
        if roomPart then
            local dist = (roomPart.Position - myPos).Magnitude
            if dist < minDistanceToRoomCenter then
                minDistanceToRoomCenter = dist
                currentRoomIdx = i
            end
        end
    end
    return currentRoomIdx
end

-- Lógica de progresión
local function AutoPassRoom()
    if not characterLoaded then return false end
    local mapFolder = Workspace:FindFirstChild("Map")
    if not mapFolder then return false end
    local dungeonFolder = mapFolder:FindFirstChild("Dungeon")
    if not dungeonFolder then return false end
    
    local rooms = dungeonFolder:GetChildren()
    if #rooms == 0 then return false end
    
    table.sort(rooms, function(a, b)
        local numA = tonumber(string.match(a.Name, "%d+")) or 0
        local numB = tonumber(string.match(b.Name, "%d+")) or 0
        return numA < numB
    end)
    
    local currentRoomIdx = GetCurrentRoomIndex(rooms)
    local activeRoom = rooms[currentRoomIdx]
    
    if activeRoom then
        local teleporter = activeRoom:FindFirstChild("ExitTeleporter")
        if teleporter then
            local rootPart = teleporter:FindFirstChild("Root")
            if rootPart and rootPart:IsA("BasePart") and humanoidRootPart then
                local touchTransmitter = rootPart:FindFirstChildOfClass("TouchTransmitter")
                if touchTransmitter then
                    humanoidRootPart.CFrame = rootPart.CFrame
                    task.wait(0.12)
                    if firetouchinterest then
                        firetouchinterest(humanoidRootPart, rootPart, 0)
                        task.wait(0.1)
                        firetouchinterest(humanoidRootPart, rootPart, 1)
                    end
                    if justDied then
                        justDied = false
                    end
                    return true
                end
            end
        end
    end
    return false
end

local function RotateTargets()
    while autoDungeonActive do
        currentTarget = GetClosestEnemy()
        
        if currentTarget and not justDied and characterLoaded then
            lastTarget = currentTarget
            local enemyHumanoid = currentTarget:FindFirstChildOfClass("Humanoid")
            local timer = 0
            
            while autoDungeonActive and currentTarget and currentTarget.Parent and enemyHumanoid and enemyHumanoid.Health > 0 and timer < 0.5 and not justDied and characterLoaded do
                TeleportToEnemy(currentTarget)
                AttackEnemy(currentTarget)
                
                task.wait(0.01)
                timer = timer + 0.01
            end
        else
            if justDied and characterLoaded then
                -- Bucle de prioridad al portal tras morir
                local passed = false
                local attempts = 0
                while autoDungeonActive and justDied and attempts < 10 and characterLoaded do
                    passed = AutoPassRoom()
                    if passed then
                        justDied = false 
                        break
                    end
                    attempts = attempts + 1
                    task.wait(0.5)
                end
                if attempts >= 10 then
                    justDied = false
                end
            else
                task.wait(1.0)
                if characterLoaded then
                    local passed = AutoPassRoom()
                    if not passed then
                        task.wait(0.5)
                    end
                end
            end
        end
        task.wait(0.05)
    end
end

local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
    Name = "⚔️ Edsusito Dungeon ⚔️",
    LoadingTitle = "Iniciando Sistema de Rotación...",
    LoadingSubtitle = "By Edsu",
    ConfigurationSaving = { Enabled = false },
    Discord = { Enabled = false },
    KeySystem = false
})

local DungeonTab = Window:CreateTab("Dungeon", 4483362458)
DungeonTab:CreateSection("Control Principal")

-- BOTÓN TOGGLE PRINCIPAL
DungeonTab:CreateToggle({
    Name = "Auto Completar",
    CurrentValue = false,
    Flag = "EdsuAutoDungeonFlag",
    Callback = function(Value)
        autoDungeonActive = Value
        if Value then
            justDied = false
            StartAutoBuffs() 
            coroutine.wrap(RotateTargets)()
        else
            currentTarget = nil
            lastTarget = nil
            justDied = false
            Rayfield:Notify({
                Title = "⚔️ Edsusito Dungeon ⚔️",
                Content = "Ciclo de rotación pausado con éxito.",
                Duration = 2,
                Image = 4483362458,
            })
        end
    end,
})

DungeonTab:CreateSection("Ajustes de Combate")

-- SLIDER DE ALTURA CONFIGURADO DESDE 10 HASTA 100 (INICIA EN 30)
DungeonTab:CreateSlider({
    Name = "Altura de Ataque",
    Range = {10, 100}, 
    Increment = 1,
    Suffix = " studs",
    CurrentValue = 30, -- Ajustado por defecto a 30 studs
    Flag = "AttackHeightFlag",
    Callback = function(Value)
        attackHeight = Value
    end,
})
